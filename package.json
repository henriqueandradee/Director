{
  "name": "diretoria",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently -n backend,frontend -c cyan,magenta --kill-others-on-fail \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
